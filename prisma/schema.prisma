generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["partialIndexes"]
}

datasource db {
  provider = "postgresql"
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bd_asesores {
  id_asesor                                                           String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  cod_asesor                                                          String?             @unique @db.VarChar(20)
  nombre_asesor                                                       String?             @db.VarChar(100)
  antiguedad                                                          Int?
  especialidad                                                        String?             @db.VarChar(50)
  edad                                                                Int?
  ventas_netas                                                        Decimal?            @db.Decimal(12, 2)
  persistencia_promedio_propios                                       Decimal?            @db.Decimal(5, 2)
  persistencia_promedio_mqs                                           Decimal?            @db.Decimal(5, 2)
  ratio_conversion_de_venta                                           Decimal?            @db.Decimal(5, 4)
  leads_en_cola                                                       Int?                @default(0)
  disponibilidad                                                      String?             @db.VarChar(20)
  fecha_creacion                                                      DateTime            @default(now()) @db.Timestamptz(6)
  fecha_actualizacion                                                 DateTime            @default(now()) @db.Timestamptz(6)
  bd_leads                                                            bd_leads[]
  crm_usuarios                                                        crm_usuarios[]
  hist_asignaciones_hist_asignaciones_id_asesor_anteriorTobd_asesores hist_asignaciones[] @relation("hist_asignaciones_id_asesor_anteriorTobd_asesores")
  hist_asignaciones_hist_asignaciones_id_asesorTobd_asesores          hist_asignaciones[] @relation("hist_asignaciones_id_asesorTobd_asesores")
  hist_clientes                                                       hist_clientes[]
  matching                                                            matching[]

  @@index([cod_asesor], map: "idx_asesores_cod")
  @@index([disponibilidad], map: "idx_asesores_disponibilidad")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model bd_leads {
  id_lead                           String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  dni                               String?                    @db.VarChar(20)
  numero                            String?                    @db.VarChar(15)
  correo                            String?                    @db.VarChar(100)
  nombre                            String?                    @db.VarChar(100)
  apellido                          String?                    @db.VarChar(100)
  producto                          String?                    @db.VarChar(50)
  zona                              String?                    @db.VarChar(50)
  origen_lead                       String?                    @db.VarChar(50)
  suborigen_lead                    String?                    @db.VarChar(50)
  linea                             String?                    @db.VarChar(50)
  scoring                           Decimal?                   @db.Decimal(5, 4)
  cluster                           Int?
  contactabilidad                   Decimal?                   @db.Decimal(3, 2)
  tasa_de_conversacion              Decimal?                   @db.Decimal(5, 4)
  ultimo_scoring                    Decimal?                   @db.Decimal(5, 4)
  probabilidad_de_conversion        Decimal?                   @db.Decimal(5, 4)
  segmento_de_scoring               String?                    @db.VarChar(20)
  ultimo_asesor_asignado            String?                    @db.Uuid
  estado_de_lead                    String?                    @default("lead") @db.VarChar(30)
  origen_de_venta                   String?                    @db.VarChar(50)
  motivo_de_descarte                String?                    @db.VarChar(100)
  ultima_fecha_gestion_bot          DateTime?                  @db.Timestamptz(6)
  ultima_fecha_gestion_convencional DateTime?                  @db.Timestamptz(6)
  cantidad_contactos_lead_bot       Int?                       @default(0)
  nivel_interes_actual              Decimal?                   @db.Decimal(3, 2)
  sentimiento_actual                String?                    @db.VarChar(20)
  nivel_intencion_de_compra         Decimal?                   @db.Decimal(3, 2)
  tiempo_respuesta_promedio         Int?
  ultimo_tiempo_de_contacto         Int?
  tiempo_sin_gestion                Int?
  id_ultima_campana                 String?                    @db.VarChar(50)
  ultimo_estado_asesor              String?                    @db.VarChar(30)
  ultimo_estado_de_contacto_asesor  String?                    @db.VarChar(30)
  antiguedad_lead                   Int?
  score_sentinel                    Decimal?                   @db.Decimal(5, 4)
  ratio_de_deuda                    Decimal?                   @db.Decimal(5, 4)
  tipo_persona                      String?                    @db.VarChar(20)
  situacion_laboral                 String?                    @db.VarChar(30)
  fecha_creacion                    DateTime                   @default(now()) @db.Timestamptz(6)
  fecha_actualizacion               DateTime                   @default(now()) @db.Timestamptz(6)
  nickname_wsp                      String?                    @db.VarChar(200)
  bd_asesores                       bd_asesores?               @relation(fields: [ultimo_asesor_asignado], references: [id_asesor], onUpdate: NoAction, map: "fk_leads_asesor")
  crm_acciones_comerciales          crm_acciones_comerciales[]
  crm_campana_leads                 crm_campana_leads[]
  crm_citas                         crm_citas[]
  crm_notas                         crm_notas[]
  crm_tareas                        crm_tareas[]
  hist_asignaciones                 hist_asignaciones[]
  hist_clientes                     hist_clientes?
  hist_conversaciones               hist_conversaciones[]
  hist_estado_asesor                hist_estado_asesor[]
  hist_scoring                      hist_scoring[]
  matching                          matching[]

  @@index([ultimo_asesor_asignado], map: "idx_leads_asesor")
  @@index([dni], map: "idx_leads_dni")
  @@index([estado_de_lead], map: "idx_leads_estado")
  @@index([numero], map: "idx_leads_numero")
  @@index([origen_lead], map: "idx_leads_origen")
  @@index([producto], map: "idx_leads_producto")
  @@index([suborigen_lead], map: "idx_leads_suborigen")
}

model config_modelo {
  id                  Int       @id @default(autoincrement())
  parametro           String    @unique @db.VarChar(50)
  valor               String    @db.VarChar(20)
  tipo_dato           String?   @db.VarChar(10)
  categoria           String?   @db.VarChar(20)
  descripcion         String?
  fecha_actualizacion DateTime? @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_campana_leads {
  id             String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_campana     String       @db.Uuid
  id_lead        String       @db.Uuid
  estado_envio   String?      @default("pendiente") @db.VarChar(20)
  fecha_envio    DateTime?    @db.Timestamptz(6)
  entregado      Boolean?     @default(false)
  leido          Boolean?     @default(false)
  respondio      Boolean?     @default(false)
  fecha_creacion DateTime     @default(now()) @db.Timestamptz(6)
  crm_campanas   crm_campanas @relation(fields: [id_campana], references: [id_campana], onDelete: Cascade, onUpdate: NoAction, map: "fk_cl_campana")
  bd_leads       bd_leads     @relation(fields: [id_lead], references: [id_lead], onDelete: Cascade, onUpdate: NoAction, map: "fk_cl_lead")

  @@unique([id_campana, id_lead], map: "uq_campana_lead")
  @@index([id_campana], map: "idx_cl_campana")
  @@index([id_lead], map: "idx_cl_lead")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_campanas {
  id_campana          String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nombre              String              @db.VarChar(200)
  base_datos          String?             @db.VarChar(100)
  filtros             String?
  cluster             String?             @default("default") @db.VarChar(50)
  estado              String              @default("Activa") @db.VarChar(20)
  total_leads         Int?                @default(0)
  id_usuario_creador  String?             @db.Uuid
  fecha_inicio        DateTime?           @db.Date
  fecha_fin           DateTime?           @db.Date
  fecha_creacion      DateTime            @default(now()) @db.Timestamptz(6)
  fecha_actualizacion DateTime            @default(now()) @db.Timestamptz(6)
  id_plantilla        String?             @db.Uuid
  variables           Json?               @default("{}")
  crm_campana_leads   crm_campana_leads[]
  crm_usuarios        crm_usuarios?       @relation(fields: [id_usuario_creador], references: [id_usuario], onUpdate: NoAction, map: "fk_campana_creador")
  crm_plantillas      crm_plantillas?     @relation(fields: [id_plantilla], references: [id_plantilla], onUpdate: NoAction, map: "fk_campana_plantilla")

  @@index([estado], map: "idx_campanas_estado")
  @@index([fecha_creacion(sort: Desc)], map: "idx_campanas_fecha")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_citas {
  id_cita                  String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  titulo                   String                     @db.VarChar(200)
  id_lead                  String?                    @db.Uuid
  nombre_lead              String?                    @db.VarChar(200)
  id_usuario               String                     @db.Uuid
  fecha                    DateTime                   @db.Date
  hora                     DateTime                   @db.Time(6)
  ubicacion                String?                    @db.VarChar(200)
  descripcion              String?
  tipo                     String                     @default("llamada") @db.VarChar(20)
  estado                   String                     @default("active") @db.VarChar(20)
  fecha_creacion           DateTime                   @default(now()) @db.Timestamptz(6)
  fecha_actualizacion      DateTime                   @default(now()) @db.Timestamptz(6)
  crm_acciones_comerciales crm_acciones_comerciales[]
  bd_leads                 bd_leads?                  @relation(fields: [id_lead], references: [id_lead], onUpdate: NoAction, map: "fk_cita_lead")
  crm_usuarios             crm_usuarios               @relation(fields: [id_usuario], references: [id_usuario], onDelete: Cascade, onUpdate: NoAction, map: "fk_cita_usuario")

  @@index([fecha, hora], map: "idx_citas_fecha")
  @@index([id_lead], map: "idx_citas_lead")
  @@index([id_usuario], map: "idx_citas_usuario")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_notas {
  id_nota        String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead        String       @db.Uuid
  id_usuario     String       @db.Uuid
  contenido      String
  tipo           String?      @default("nota") @db.VarChar(30)
  fecha_creacion DateTime     @default(now()) @db.Timestamptz(6)
  bd_leads       bd_leads     @relation(fields: [id_lead], references: [id_lead], onDelete: Cascade, onUpdate: NoAction, map: "fk_nota_lead")
  crm_usuarios   crm_usuarios @relation(fields: [id_usuario], references: [id_usuario], onDelete: Cascade, onUpdate: NoAction, map: "fk_nota_usuario")

  @@index([fecha_creacion(sort: Desc)], map: "idx_notas_fecha")
  @@index([id_lead], map: "idx_notas_lead")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_plantillas {
  id_plantilla        String         @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nombre              String         @db.VarChar(200)
  asunto              String?        @db.VarChar(300)
  contenido           String
  tipo                String?        @default("email") @db.VarChar(20)
  id_usuario_creador  String?        @db.Uuid
  fecha_creacion      DateTime       @default(now()) @db.Timestamptz(6)
  fecha_actualizacion DateTime       @default(now()) @db.Timestamptz(6)
  crm_campanas        crm_campanas[]
  crm_usuarios        crm_usuarios?  @relation(fields: [id_usuario_creador], references: [id_usuario], onUpdate: NoAction, map: "fk_plantilla_creador")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_tareas {
  id_tarea            String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead             String       @db.Uuid
  id_usuario_asignado String       @db.Uuid
  titulo              String       @db.VarChar(200)
  descripcion         String?
  prioridad           String       @default("Media") @db.VarChar(10)
  estado              String       @default("pendiente") @db.VarChar(20)
  fecha_vencimiento   DateTime?    @db.Date
  fecha_completada    DateTime?    @db.Timestamptz(6)
  fecha_creacion      DateTime     @default(now()) @db.Timestamptz(6)
  fecha_actualizacion DateTime     @default(now()) @db.Timestamptz(6)
  bd_leads            bd_leads     @relation(fields: [id_lead], references: [id_lead], onDelete: Cascade, onUpdate: NoAction, map: "fk_tarea_lead")
  crm_usuarios        crm_usuarios @relation(fields: [id_usuario_asignado], references: [id_usuario], onDelete: Cascade, onUpdate: NoAction, map: "fk_tarea_usuario")

  @@index([estado], map: "idx_tareas_estado")
  @@index([id_lead], map: "idx_tareas_lead")
  @@index([prioridad], map: "idx_tareas_prioridad")
  @@index([id_usuario_asignado], map: "idx_tareas_usuario")
  @@index([fecha_vencimiento], map: "idx_tareas_vencimiento")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_usuarios {
  id_usuario               String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  username                 String                     @unique @db.VarChar(50)
  nombre                   String                     @db.VarChar(100)
  email                    String                     @unique @db.VarChar(150)
  password_hash            String                     @db.VarChar(255)
  rol                      String                     @default("Asesor") @db.VarChar(20)
  activo                   Boolean?                   @default(true)
  id_asesor                String?                    @db.Uuid
  fecha_ingreso            DateTime?                  @default(dbgenerated("CURRENT_DATE")) @db.Date
  ultimo_login             DateTime?                  @db.Timestamptz(6)
  fecha_creacion           DateTime                   @default(now()) @db.Timestamptz(6)
  fecha_actualizacion      DateTime                   @default(now()) @db.Timestamptz(6)
  crm_acciones_comerciales crm_acciones_comerciales[]
  crm_campanas             crm_campanas[]
  crm_citas                crm_citas[]
  crm_notas                crm_notas[]
  crm_plantillas           crm_plantillas[]
  crm_tareas               crm_tareas[]
  bd_asesores              bd_asesores?               @relation(fields: [id_asesor], references: [id_asesor], onUpdate: NoAction, map: "fk_usuario_asesor")
  hist_estado_asesor       hist_estado_asesor[]

  @@index([activo], map: "idx_usuarios_activo")
  @@index([rol], map: "idx_usuarios_rol")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model hist_asignaciones {
  id_asignacion                                                 String                     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead                                                       String                     @db.Uuid
  id_asesor                                                     String                     @db.Uuid
  id_matching                                                   String?                    @db.Uuid
  score_total_al_asignar                                        Decimal?                   @db.Decimal(5, 4)
  score_k_al_asignar                                            Decimal?                   @db.Decimal(5, 4)
  score_c_al_asignar                                            Decimal?                   @db.Decimal(5, 4)
  score_v_al_asignar                                            Decimal?                   @db.Decimal(5, 4)
  score_p_al_asignar                                            Decimal?                   @db.Decimal(5, 4)
  estado_gestion                                                String?                    @default("en_espera") @db.VarChar(30)
  motivo_descarte                                               String?                    @db.VarChar(100)
  fecha_asignacion                                              DateTime                   @default(now()) @db.Timestamptz(6)
  fecha_primer_contacto                                         DateTime?                  @db.Timestamptz(6)
  tiempo_espera_horas                                           Decimal?                   @db.Decimal(6, 2)
  fecha_cambio_estado                                           DateTime?                  @db.Timestamptz(6)
  cerro_venta                                                   Boolean?                   @default(false)
  monto_venta                                                   Decimal?                   @db.Decimal(12, 2)
  producto_vendido                                              String?                    @db.VarChar(50)
  reasignado                                                    Boolean?                   @default(false)
  id_asesor_anterior                                            String?                    @db.Uuid
  motivo_reasignacion                                           String?                    @db.VarChar(100)
  reciclado_a_bot                                               Boolean?                   @default(false)
  crm_acciones_comerciales                                      crm_acciones_comerciales[]
  bd_asesores_hist_asignaciones_id_asesor_anteriorTobd_asesores bd_asesores?               @relation("hist_asignaciones_id_asesor_anteriorTobd_asesores", fields: [id_asesor_anterior], references: [id_asesor], onDelete: NoAction, onUpdate: NoAction)
  bd_asesores_hist_asignaciones_id_asesorTobd_asesores          bd_asesores                @relation("hist_asignaciones_id_asesorTobd_asesores", fields: [id_asesor], references: [id_asesor], onDelete: NoAction, onUpdate: NoAction)
  bd_leads                                                      bd_leads                   @relation(fields: [id_lead], references: [id_lead], onDelete: NoAction, onUpdate: NoAction)
  matching                                                      matching?                  @relation(fields: [id_matching], references: [id_matching], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_asesor], map: "idx_asig_asesor")
  @@index([cerro_venta], map: "idx_asig_cerro", where: raw("(cerro_venta = true)"))
  @@index([estado_gestion], map: "idx_asig_estado")
  @@index([fecha_asignacion(sort: Desc)], map: "idx_asig_fecha")
  @@index([id_lead], map: "idx_asig_lead")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model hist_clientes {
  id_cliente          String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead             String      @unique @db.Uuid
  id_asesor_cierre    String      @db.Uuid
  fecha_compra        DateTime    @db.Timestamptz(6)
  producto_comprado   String?     @db.VarChar(50)
  monto_venta         Decimal?    @db.Decimal(12, 2)
  estado_cliente      String?     @default("activo") @db.VarChar(20)
  meses_activo        Int?        @default(0)
  cantidad_pagos      Int?        @default(0)
  pagos_atrasados     Int?        @default(0)
  monto_total_pagado  Decimal?    @default(0) @db.Decimal(12, 2)
  riesgo_churn        Decimal?    @db.Decimal(5, 4)
  clv_real            Decimal?    @db.Decimal(12, 2)
  clv_predicho        Decimal?    @db.Decimal(12, 2)
  fecha_cancelacion   DateTime?   @db.Timestamptz(6)
  motivo_cancelacion  String?     @db.VarChar(100)
  fecha_actualizacion DateTime?   @default(now()) @db.Timestamptz(6)
  bd_asesores         bd_asesores @relation(fields: [id_asesor_cierre], references: [id_asesor], onDelete: NoAction, onUpdate: NoAction)
  bd_leads            bd_leads    @relation(fields: [id_lead], references: [id_lead], onDelete: NoAction, onUpdate: NoAction)

  @@index([id_asesor_cierre], map: "idx_clientes_asesor")
  @@index([estado_cliente], map: "idx_clientes_estado")
  @@index([fecha_compra(sort: Desc)], map: "idx_clientes_fecha")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model hist_conversaciones {
  id_mensaje           String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead              String   @db.Uuid
  id_campana           String?  @db.VarChar(50)
  direccion            String?  @db.VarChar(10)
  contenido            String?
  tipo_mensaje         String?  @db.VarChar(30)
  canal                String?  @db.VarChar(20)
  entregado            Boolean?
  leido                Boolean?
  respondio            Boolean?
  tiempo_respuesta_seg Int?
  sentimiento          String?  @db.VarChar(20)
  intencion            String?  @db.VarChar(30)
  keywords             Json?
  score_antes          Decimal? @db.Decimal(5, 4)
  score_despues        Decimal? @db.Decimal(5, 4)
  delta_score          Decimal? @db.Decimal(5, 4)
  timestamp            DateTime @default(now()) @db.Timestamptz(6)
  bd_leads             bd_leads @relation(fields: [id_lead], references: [id_lead], onDelete: NoAction, onUpdate: NoAction)

  @@index([direccion], map: "idx_conv_direccion")
  @@index([intencion], map: "idx_conv_intencion")
  @@index([id_lead], map: "idx_conv_lead")
  @@index([timestamp(sort: Desc)], map: "idx_conv_timestamp")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model hist_scoring {
  id_hist          String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead          String   @db.Uuid
  scoring_anterior Decimal? @db.Decimal(5, 4)
  scoring_nuevo    Decimal? @db.Decimal(5, 4)
  delta_scoring    Decimal? @db.Decimal(5, 4)
  evento_trigger   String?  @db.VarChar(50)
  nivel_interes    Decimal? @db.Decimal(3, 2)
  sentimiento      String?  @db.VarChar(20)
  contactabilidad  Decimal? @db.Decimal(3, 2)
  timestamp        DateTime @default(now()) @db.Timestamptz(6)
  bd_leads         bd_leads @relation(fields: [id_lead], references: [id_lead], onDelete: NoAction, onUpdate: NoAction)

  @@index([evento_trigger], map: "idx_scoring_evento")
  @@index([id_lead], map: "idx_scoring_lead")
  @@index([timestamp(sort: Desc)], map: "idx_scoring_timestamp")
}

model matching {
  id_matching       String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead           String              @db.Uuid
  id_asesor         String              @db.Uuid
  score_k           Decimal?            @db.Decimal(5, 4)
  score_c           Decimal?            @db.Decimal(5, 4)
  score_v           Decimal?            @db.Decimal(5, 4)
  score_p           Decimal?            @db.Decimal(5, 4)
  score_total       Decimal?            @db.Decimal(5, 4)
  peso_w1           Decimal?            @db.Decimal(3, 2)
  peso_w2           Decimal?            @db.Decimal(3, 2)
  peso_w3           Decimal?            @db.Decimal(3, 2)
  peso_w4           Decimal?            @db.Decimal(3, 2)
  asignado          Boolean?            @default(false)
  fecha_evaluacion  DateTime            @default(now()) @db.Timestamptz(6)
  fecha_asignacion  DateTime?           @db.Timestamptz(6)
  hist_asignaciones hist_asignaciones[]
  bd_asesores       bd_asesores         @relation(fields: [id_asesor], references: [id_asesor], onDelete: NoAction, onUpdate: NoAction)
  bd_leads          bd_leads            @relation(fields: [id_lead], references: [id_lead], onDelete: NoAction, onUpdate: NoAction)

  @@unique([id_lead, id_asesor, fecha_evaluacion], map: "uq_matching_lead_asesor_fecha")
  @@index([asignado], map: "idx_matching_asignado", where: raw("(asignado = true)"))
  @@index([fecha_evaluacion(sort: Desc)], map: "idx_matching_fecha")
  @@index([id_lead], map: "idx_matching_lead")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crm_acciones_comerciales {
  id_accion          String               @id(map: "crm_acciones_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead            String               @db.Uuid
  id_asignacion      String?              @db.Uuid
  id_usuario         String               @db.Uuid
  tipo_accion        String               @db.VarChar(30)
  estado_asesor      String               @db.VarChar(30)
  observaciones      String?
  id_cita            String?              @db.Uuid
  duracion_seg       Int?
  fecha_creacion     DateTime             @default(now()) @db.Timestamptz(6)
  hist_asignaciones  hist_asignaciones?   @relation(fields: [id_asignacion], references: [id_asignacion], onUpdate: NoAction, map: "fk_accion_asignacion")
  crm_citas          crm_citas?           @relation(fields: [id_cita], references: [id_cita], onUpdate: NoAction, map: "fk_accion_cita")
  bd_leads           bd_leads             @relation(fields: [id_lead], references: [id_lead], onDelete: Cascade, onUpdate: NoAction, map: "fk_accion_lead")
  crm_usuarios       crm_usuarios         @relation(fields: [id_usuario], references: [id_usuario], onDelete: Cascade, onUpdate: NoAction, map: "fk_accion_usuario")
  hist_estado_asesor hist_estado_asesor[]

  @@index([id_asignacion], map: "idx_acciones_asignacion")
  @@index([estado_asesor], map: "idx_acciones_estado")
  @@index([fecha_creacion(sort: Desc)], map: "idx_acciones_fecha")
  @@index([id_lead], map: "idx_acciones_lead")
  @@index([tipo_accion], map: "idx_acciones_tipo")
  @@index([id_usuario], map: "idx_acciones_usuario")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model hist_estado_asesor {
  id_hist                  String                   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  id_lead                  String                   @db.Uuid
  id_accion                String                   @db.Uuid
  id_usuario               String                   @db.Uuid
  estado_anterior          String?                  @db.VarChar(30)
  estado_nuevo             String                   @db.VarChar(30)
  fecha_cambio             DateTime                 @default(now()) @db.Timestamptz(6)
  crm_acciones_comerciales crm_acciones_comerciales @relation(fields: [id_accion], references: [id_accion], onDelete: Cascade, onUpdate: NoAction, map: "fk_hist_ea_accion")
  bd_leads                 bd_leads                 @relation(fields: [id_lead], references: [id_lead], onDelete: Cascade, onUpdate: NoAction, map: "fk_hist_ea_lead")
  crm_usuarios             crm_usuarios             @relation(fields: [id_usuario], references: [id_usuario], onDelete: Cascade, onUpdate: NoAction, map: "fk_hist_ea_usuario")

  @@index([estado_nuevo], map: "idx_hist_ea_estado")
  @@index([fecha_cambio(sort: Desc)], map: "idx_hist_ea_fecha")
  @@index([id_lead], map: "idx_hist_ea_lead")
}

model mensaje_status_event {
  id                BigInt   @id @default(autoincrement())
  id_msg            String?
  estado            String?
  ts_unix           BigInt?
  recipient_id      String?
  pricing_json      Json?
  conversation_json Json?
  errors_json       Json?
  created_at        DateTime @default(now()) @db.Timestamp(6)

  @@index([created_at], map: "idx_mse_created")
  @@index([estado], map: "idx_mse_estado")
  @@index([id_msg], map: "idx_mse_id_msg")
}

model webhook_logs {
  id         Int      @id @default(autoincrement())
  event_type String   @default("unknown") @db.VarChar(100)
  payload    Json?
  created_at DateTime @default(now()) @db.Timestamptz(6)
  wamid      String?

  @@index([created_at], map: "idx_webhook_logs_created")
  @@index([event_type], map: "idx_webhook_logs_event")
  @@index([wamid], map: "idx_webhook_logs_wamid")
}
